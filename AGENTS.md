# 项目协作约定

本项目用于学生按阶段实现研发问题诊断 Agent 与回归评测。

- 当前是空骨架；从 docs/09-progress.md 确认进度，从 docs/07-backlog.md 领取一个小任务。
- 按用户当前要求推进；通常先说明本次学习目标，再实现对应范围，避免一次生成全部系统。
- 完成任务后记录改动、验证、限制与下一步；不要把模板、虚构样例、跳过的测试算作成果。
- 业务代码放 src/diagnosis_agent，普通测试放 tests，DeepEval 评测放 evals。
- 不导入无关目录、凭据或整个磁盘的内容；初期知识来源为人工选取的项目资料。
- `.env`、本地日志和原始问题材料不提交；只有脱敏后的知识、用例与报告才进入版本管理。
- 引入上游代码时记录来源文件、commit SHA、许可和个人改动，保留要求的版权声明。
- 当前没有模型服务、工具执行服务或测试实现；缺失条件要明确报告，不能生成伪造通过结果。
- 回归中区分业务失败、基础设施错误、跳过、未运行；不得通过删除断言使报告变绿。
- 通用错误和纠正记入 .learnings；具体实验写入 notes 或 reports，避免重复记录敏感内容。
